# AGENTS.md

このフォルダ全体では、非エンジニア初心者が Codex を使って、HTML / CSS / JavaScript 中心の小さな静的Webページ、LP、簡単なWebツールを安全に作ることを前提に作業します。

このプロジェクトでは「初回相談ヒアリングシート生成ツール」を作ります。小規模事業者や個人事業主への初回相談前に、必要な質問項目、確認事項、次回アクションを整理できる静的Webツールを目指します。

## 基本方針

- ユーザーは非エンジニア初心者として扱います。
- 専門用語は必要な場合だけ使い、できるだけ短く分かりやすく説明します。
- HTML / CSS / JavaScript を中心に、小さく作って確認しやすい形を優先します。
- 1フォルダ1作品で管理します。
- 既存機能を壊さないことを優先します。
- 外部ライブラリ、サーバー、APIは原則使いません。
- 公開して困る個人情報、秘密情報、内部情報は入れません。

## 作業前ルール

ファイルを変更する前に、次の内容を簡潔に説明します。

- どのファイルを変更する予定か
- 何を追加・修正する予定か
- 既存機能への影響がありそうか

ユーザーが「レビューだけ」「確認だけ」「変更しないで」と言った場合は、絶対にファイルを変更しません。確認結果と提案だけを伝えます。

次の場合は、作業前に理由と影響を説明します。

- 大きな仕様変更を行う場合
- 設計変更を行う場合
- 大規模リファクタリングを行う場合
- 外部ライブラリ、サーバー、APIが必要になりそうな場合
- 静的サイトだけでは完結しにくい機能を扱う場合

プロジェクト外の無関係なファイルは変更しません。

## ファイル構成ルール

原則として、次の役割でファイルを分けます。

- `index.html`: 画面構造
- `style.css`: 見た目
- `script.js`: 動き・処理。必要な場合のみ作成します。
- `README.md`: 概要、使い方、注意点、確認手順、更新方針

特別な理由がない限り、HTML・CSS・JavaScript を1つのファイルにまとめません。

HTML / CSS だけで済む静的ページでは、不要に `script.js` を追加しません。

フォーム送信、ログイン、外部保存、複数端末同期など、静的サイトだけでは完結しにくい機能は、実装前に理由と選択肢を説明します。

## README更新ルール

ページ内容、機能、公開URL、使い方、注意点を変更した場合は、`README.md` も確認します。

`README.md` には、必要に応じて次の内容を書きます。

- プロジェクト概要
- 対象ユーザー
- できること
- ファイル構成
- 使い方
- 動作確認手順
- 注意点
- 今後の改善案

`localStorage` を使う場合は、ブラウザ内保存であり、別端末や別ブラウザには同期されないことを `README.md` に書きます。

## このツールの位置づけ

- このツールは「情報を大量に集めるツール」ではなく、「初回相談前に聞くべきことを整理するツール」として作ります。
- 断定的な診断や助言ではなく、相談前の整理、質問作成、確認事項の洗い出しを目的にします。
- 利用者が相談内容を整理し、初回相談をスムーズに進めるための補助ツールとして扱います。

## 個人情報・機密情報の扱い

- 氏名、住所、電話番号、メールアドレス、口座情報、契約情報、詳細な売上、顧客名などの個人情報・機密情報を必要以上に入力させません。
- 入力欄は、相談テーマ、事業の概要、困っていること、希望する支援内容など、初回相談前の整理に必要な範囲に限定します。
- 個人や取引先が特定される情報を必須入力にしません。
- サンプル文には、実在の個人名、会社名、住所、電話番号、メールアドレスを使いません。

## 専門判断の回避

- 医療、法律、税務、労務、投資、許認可などの専門判断をツールが断定しません。
- 専門判断が必要な内容は「専門家に確認する項目」として整理します。
- 「診断します」「判断します」「最適解を提示します」のような表現は避けます。
- 「確認する」「聞いてみる」「整理する」「相談する」などの表現を使います。

## 出力形式のルール

生成結果は原則として、次の3区分に分けます。

- 質問リスト
- 確認事項
- 次回アクション

必要に応じて「専門家に確認する項目」も追加します。

出力は、初回相談でそのまま使いやすい短い箇条書きを中心にします。

初心者にも分かる表現にします。

## コピー機能・履歴機能の注意

- コピー機能を入れる場合は、コピー前後に個人情報や機密情報が含まれていないか確認する注意を `README.md` に書きます。
- 履歴機能を入れる場合は、ブラウザ内保存であること、同じ端末を使う人に見られる可能性があること、削除ボタンを用意することを `README.md` に書きます。
- 履歴機能を入れる場合は、履歴削除機能を用意します。
- 共有端末で使う可能性を考え、保存した内容を利用者が自分で消せる設計にします。

## READMEに必ず書く注意点

このプロジェクトの `README.md` には、次の注意点を必ず書きます。

- このツールは初回相談前のヒアリング項目を整理するための補助ツールであり、専門的な判断や助言を行うものではないこと。
- 医療、法律、税務、労務、投資、許認可などに関わる内容は、必ず専門家に確認すること。
- 個人情報や機密情報は、必要最小限の入力にとどめること。
- 入力内容や生成結果をコピー・共有する場合は、個人情報や秘密情報が含まれていないか確認すること。
- 履歴機能を使う場合、保存先はブラウザ内であり、別端末・別ブラウザには同期されないこと。
- 共有端末では履歴が他の人に見られる可能性があるため、利用後に削除すること。
- このツールは静的Webページとして動作し、外部サーバーへの保存や送信は行わない設計にすること。

## 静的公開前チェックの基本ルール

公開前や共有前には、必要に応じて次の点を確認します。

- `index.html` / `style.css` / `script.js` / `README.md` の必要ファイルがそろっているか
- 作業指示用ファイルや不要ファイルを公開用フォルダに含めていないか
- スマホ幅でも表示が崩れていないか
- 外部リンクが正しく開くか
- `href=""`、`href="mailto:"`、`href="#"` のような未完成リンクが残っていないか
- 「仮」「TODO」「未実装」「公開URLをここに入力」などの仮文言が残っていないか
- `README.md` の内容が最新か
- コピー機能や履歴機能がある場合は、公開後URLでも動作確認する

## Git / GitHub 操作ルール

- 変更後は `git status` と `git diff` を確認します。
- `git add` / `git commit` の前に、変更ファイルと変更内容を要約します。
- コミットメッセージは、変更内容が分かる短いものにします。
- Codex は `git push` を実行しません。
- push はユーザーが PowerShell で実行します。
- APIキー、パスワード、個人情報、顧客情報などが含まれている可能性がある場合は、コミットせずに停止します。

## Netlify運用ルール

- GitHubリポジトリとNetlifyを連携して公開します。
- Build command は原則空欄にします。
- Publish directory は原則 `.` にします。
- 公開対象はプロジェクト直下の `index.html` とします。
- push後はNetlifyのデプロイ状況を確認します。
- 公開URLで、表示・入力・出力・コピー機能・スマホ表示を確認します。

## アクセシビリティ・見やすさの基本ルール

- 見出しの順番を大きく崩しません。
- ボタンやリンクの文言は、クリック後に何が起きるか分かる表現にします。
- スマホでも文字が小さすぎないようにします。
- 色だけに頼らず、文章でも意味が分かるようにします。
- 文字がボタンやカードからはみ出さないようにします。
- コメントは複雑な処理にだけ入れます。HTML / CSS に不要なコメントを増やしすぎません。

## 作業中の注意

- 既存の見た目や動きを不用意に変えません。
- すでにあるコードの意図を確認してから変更します。
- ユーザーの許可なく大規模リファクタリングをしません。
- 外部ライブラリを勝手に追加しません。
- サーバーやAPIが必要な構成へ勝手に変更しません。
- 公開して困る内部情報や個人情報を入れません。
- 作品ごとのフォルダ外にある無関係なファイルは変更しません。

## 作業後ルール

作業後は、次の内容を初心者にも分かる言葉で説明します。

- 変更したファイル
- 変更内容
- 動作確認手順
- `README.md` を更新したかどうか
- 未確認事項
- 次に確認すべき点

動作確認の手順は、できるだけ具体的に書きます。

例:

1. 作品フォルダの `index.html` をブラウザで開く
2. ボタンをクリックする
3. 結果が画面に表示されることを確認する

## レビューだけの依頼

ユーザーが「レビューだけ」「確認だけ」「変更しないで」と言った場合は、ファイルを変更しません。

その場合は、次のように返します。

- 気づいた問題点
- 初心者向けの分かりやすい理由
- 修正するならどこをどう直すか

実際の修正は、ユーザーが変更を依頼してから行います。
